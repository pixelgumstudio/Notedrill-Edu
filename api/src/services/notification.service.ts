/** Notification service stub — Firebase removed for Edu platform. */

const notificationService = {
  sendJobCompletionNotification: async (..._args: any[]) => {},
  sendNotification: async (..._args: any[]) => {},
};

export default notificationService;
